// Roles disponibles dans l'application
// admin -> acces au tableau de bord administrateur (gestion globale)
// client -> acces a l'espace client (ses propres reservations et son profil)
export enum Role {
  ADMIN = 'admin',
  CLIENT = 'client',
}
